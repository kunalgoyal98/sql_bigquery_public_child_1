# ==================
# Project Details
# ==================
name: "SQL_BigquerySharedBasic"
config-version: 2
require-dbt-version:
- ">=1.0.0"
- "<2.0.0"
version: "0.1"
profile: "prophecy-default"

# ==================
# File Path Configs
# ==================
clean-targets:
- "target"
- "dbt_modules"
- "logs"
seed-paths:
- "seeds"
model-paths:
- "models"
- "prophecy-sources"
macro-paths:
- "macros"
target-path: "target"

# ==================
# Variable Configs
# ==================
vars:
  v_project_int: '11'
  v_project_dict: '{''a'': 2, ''b'': ''hello''}'
  test_var: hello sir mister

# ============
# Misc
# ============
dispatch: []
